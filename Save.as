package {	import flash.display.*;	import flash.events.*;	import flash.net.*;	//import fl.controls.*;			public class Save extends EventDispatcher{				private var phpFile:String;		private var saveToFile:String;		private var req:URLRequest;		private var loader:URLLoader = new URLLoader();		private var vars:URLVariables = new URLVariables();		private var done:Boolean;				public function Save(){								}				public function saveit(php:String, saveFile:String, saveContent):void{						phpFile = php;			saveToFile = saveFile;			req = new URLRequest(phpFile);			vars.content = saveContent; // put this in php			vars.fileName = saveToFile;// put this in php			req.data = vars;			loader.load(req);			loader.addEventListener(Event.COMPLETE, checkIt);			 			 			 		}				private function checkIt(event:Event){			if(loader == vars.content){				done = true;			}			dispatchEvent(new Event (Event.COMPLETE) );		}			}	}